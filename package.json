{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "nohoist": [
        "**/typeorm",
        "**/typeorm/**"
    ],
    "devDependencies": {
        "husky": "^6.0.0"
    },
    "scripts": {
        "prepare": "husky install",
        "test": "yarn install && cd packages/server  && yarn test"
    }
}
